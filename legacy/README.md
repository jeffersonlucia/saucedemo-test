# Legacy — SauceDemo Cypress

Projeto anterior de testes E2E (SauceDemo). Mantido apenas como referência de automação.

```bash
cd legacy
npm install
npm run cypress:run
```

O produto ativo do repositório é **Whiskey Club OS** (raiz).
